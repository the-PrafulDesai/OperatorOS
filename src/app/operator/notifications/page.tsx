import { NotificationsList } from "@/components/common/notifications-list";import { PageHeader } from "@/components/common/page-header";import { requireRole } from "@/lib/auth/require-role";import { getNotifications } from "@/lib/data/phase3";
export const metadata={title:"Notifications | OperatorOS"};export default async function OperatorNotificationsPage(){const profile=await requireRole("OPERATOR_ADMIN");const items=await getNotifications(profile.id).catch(()=>[]);return <div className="page-container"><PageHeader eyebrow="Booking operations" title="Notifications" description="New reservations and customer-impacting booking updates."/><div className="mx-auto mt-6 max-w-3xl"><NotificationsList items={items}/></div></div>}
