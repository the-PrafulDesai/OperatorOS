import Link from"next/link";import{CalendarDays,ChevronRight}from"lucide-react";import{format}from"date-fns";import{PageHeader}from"@/components/common/page-header";import{StatusBadge}from"@/components/common/status-badge";import{Input}from"@/components/ui/input";import{Select,SelectContent,SelectItem,SelectTrigger,SelectValue}from"@/components/ui/select";import{requireRole}from"@/lib/auth/require-role";import{getOperatorBookings}from"@/lib/data/phase3";import{bookingSchedule}from"@/lib/booking/presentation";import{currency,productTypeLabel}from"@/lib/products/labels";import type{BookingStatus,WorkspaceProductType}from"@/types/database";
export const metadata={title:"Bookings | OperatorOS"};export default async function OperatorBookingsPage({searchParams}:{searchParams:Promise<{q?:string;status?:BookingStatus;category?:WorkspaceProductType}>}){const profile=await requireRole("OPERATOR_ADMIN");const filters=await searchParams;let rows=await getOperatorBookings(profile.id).catch(()=>[]);if(filters.q){const q=filters.q.toLowerCase();rows=rows.filter(b=>`${b.booking_reference} ${b.customer?.full_name} ${b.customer?.email} ${b.product?.name}`.toLowerCase().includes(q))}if(filters.status&&filters.status!==("ALL" as BookingStatus))rows=rows.filter(b=>b.status===filters.status);if(filters.category&&filters.category!==("ALL" as WorkspaceProductType))rows=rows.filter(b=>b.product_type===filters.category);return <div className="page-container"><PageHeader eyebrow="Booking operations" title="Bookings" description="Fulfil confirmed reservations for your assigned location."/><form className="mt-6 grid gap-3 rounded-2xl border bg-white p-4 sm:grid-cols-[1fr_200px_220px]"><Input name="q" defaultValue={filters.q} placeholder="Search reference, customer, product"/><Select name="status" defaultValue={filters.status??"ALL"}><SelectTrigger className="w-full"><SelectValue/></SelectTrigger><SelectContent><SelectItem value="ALL">All statuses</SelectItem>{["CONFIRMED","CHECKED_IN","COMPLETED","REFUNDED","NO_SHOW"].map(v=><SelectItem key={v} value={v}>{v.replaceAll("_"," ")}</SelectItem>)}</SelectContent></Select><Select name="category" defaultValue={filters.category??"ALL"}><SelectTrigger className="w-full"><SelectValue/></SelectTrigger><SelectContent><SelectItem value="ALL">All categories</SelectItem>{Object.entries(productTypeLabel).map(([v,l])=><SelectItem key={v} value={v}>{l}</SelectItem>)}</SelectContent></Select></form>{rows.length?<div className="mt-6 overflow-hidden rounded-2xl border bg-white"><div className="divide-y">{rows.map(b=><Link key={b.id} href={`/operator/bookings/${b.id}`} className="grid gap-3 p-5 transition hover:bg-slate-50 sm:grid-cols-[1.4fr_1fr_1fr_auto] sm:items-center"><div><div className="flex flex-wrap items-center gap-2"><p className="font-semibold">{b.booking_reference}</p><StatusBadge status={b.status}/></div><p className="mt-1 text-sm text-muted-foreground">{b.customer?.full_name} · {b.product?.name}</p></div><div><p className="text-xs text-muted-foreground">Schedule</p><p className="mt-1 text-sm">{bookingSchedule(b)}</p></div><div><p className="text-xs text-muted-foreground">Customer / earnings</p><p className="mt-1 text-sm font-medium">{currency(b.total_amount)} / {currency(b.operator_earnings)}</p><p className="mt-1 text-xs text-muted-foreground">{format(new Date(b.created_at),"d MMM yyyy")}</p></div><ChevronRight className="text-muted-foreground"/></Link>)}</div></div>:<div className="mt-6 rounded-3xl border border-dashed bg-white p-14 text-center"><CalendarDays className="mx-auto size-10 text-muted-foreground"/><h2 className="mt-4 font-semibold">No bookings match</h2><p className="mt-2 text-sm text-muted-foreground">New confirmed reservations will appear automatically.</p></div>}</div>}
