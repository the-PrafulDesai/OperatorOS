import { PageHeader } from "@/components/common/page-header"; import { ProductForm } from "@/components/operator/products/product-form"; import { getOperatorWorkspace } from "@/lib/data/phase2"; import { requireRole } from "@/lib/auth/require-role";
export const metadata={title:"Add Workspace Product | OperatorOS"};export default async function NewProductPage(){const profile=await requireRole("OPERATOR_ADMIN");const workspace=await getOperatorWorkspace(profile.id);if(!workspace)return null;return <main className="page-container max-w-5xl"><PageHeader eyebrow="Guided product setup" title="Add workspace product" description="Build a customer-ready offer with the right pricing, availability, inventory, and media model."/><ProductForm workspace={workspace}/></main>}
